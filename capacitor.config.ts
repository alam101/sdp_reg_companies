import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SDP_registration',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
